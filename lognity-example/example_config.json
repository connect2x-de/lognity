{
    "$schema": "../schema/Lognity Config V1.json",
    "version": 1,
    "level": "{DEFAULT_LOG_LEVEL}",
    "enabled": true,
    "sanitization_mode": "OBFUSCATE",
    "overrides": [
        {
            "condition": {
                "type": "logger_name",
                "condition": "CONTAINS",
                "value": "com.example.logger"
            },
            "level": "TRACE"
        }
    ],
    "appenders": [
        {
            "type": "console",
            "name": "my_appender",
            "pattern": "{{levelColor}}>>  {{levelSymbol}}\t{{hh}}:{{mm}}:{{ss}}.{{SSS}} ({{name}} @ {{threadId}}) {{message}}{{r}}",
            "formatter": "default"
        },
        {
            "type": "rolling_file",
            "name": "my_file_appender",
            "base_path": "{LOG_DIRECTORY}/example.log",
            "pattern": "[{{level}}] {{hh}}:{{mm}}:{{ss}}.{{SSS}} ({{name}} @ {{threadId}}) {{message}}{{r}}",
            "formatter": "default",
            "filter": {
                "conditions": [
                    {
                        "type": "marker",
                        "condition": "KEY_NOT_EQUALS",
                        "value": "marker1"
                    },
                    {
                        "type": "logger_name",
                        "condition": "NOT_CONTAINS",
                        "value": "My Filtered Logger"
                    }
                ]
            }
        }
    ]
}